spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://git.oschina.net/leejean/config/
#          search-paths: repo
#          username:
#          password:

server:
  port: 1201
